<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="book.app">

    <application
        android:allowBackup="true"
        android:icon="@drawable/read"
        android:label="个人图书管理"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.MainActivity" />
        <activity
            android:name=".app.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddBooksActivity" />
        <activity android:name=".activity.DeleteBooksActivity" />
        <activity android:name=".activity.UpdateBooksActivity" />
        <activity android:name=".activity.SearchBooksActivity" />
    </application>

</manifest>